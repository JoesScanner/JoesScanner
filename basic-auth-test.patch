--- a/JoesScanner/Models/CallStreamTestResult.cs
+++ b/JoesScanner/Models/CallStreamTestResult.cs
@@ -0,0 +1,7 @@
+using System;
+
+namespace JoesScanner.Models
+{
+    public sealed record CallStreamTestResult(bool Success, string Message, int? StatusCode = null);
+}
--- a/JoesScanner/Services/ICallStreamService.cs
+++ b/JoesScanner/Services/ICallStreamService.cs
@@ -1,9 +1,14 @@
+using JoesScanner.Models;
+
 namespace JoesScanner.Services;
 
 public interface ICallStreamService
 {
     IAsyncEnumerable<CallItem> GetCallStreamAsync(CancellationToken cancellationToken);
+
+    // Runs a lightweight connectivity check against the configured Server URL using the current Basic Auth settings (if applicable).
+    Task<CallStreamTestResult> TestCallStreamAsync(CancellationToken cancellationToken = default);
 }
--- a/JoesScanner/Services/CallStreamService.cs
+++ b/JoesScanner/Services/CallStreamService.cs
@@ -1,6 +1,7 @@
 using System.Net.Http.Headers;
 using System.Text;
 using System.Text.Json;
+using JoesScanner.Models;
 
 namespace JoesScanner.Services
 {
@@ -58,6 +59,71 @@ namespace JoesScanner.Services
             _httpClient = httpClient;
         }
 
+        public async Task<CallStreamTestResult> TestCallStreamAsync(CancellationToken cancellationToken = default)
+        {
+            var serverUrl = _settingsService.ServerUrl?.Trim() ?? string.Empty;
+            if (string.IsNullOrWhiteSpace(serverUrl))
+                return new CallStreamTestResult(false, "Server URL is empty.");
+
+            if (!Uri.TryCreate(serverUrl, UriKind.Absolute, out var serverUri))
+                return new CallStreamTestResult(false, "Server URL is not a valid absolute URL.");
+
+            try
+            {
+                UpdateAuthorizationHeader(serverUrl);
+
+                var callsUri = new Uri(serverUri, "/calljson");
+                var requestData = new Dictionary<string, string>
+                {
+                    ["draw"] = "1",
+                    ["start"] = "0",
+                    ["length"] = "1",
+                    ["search[value]"] = "",
+                    ["search[regex]"] = "false"
+                };
+
+                using var request = new HttpRequestMessage(HttpMethod.Post, callsUri)
+                {
+                    Content = new FormUrlEncodedContent(requestData)
+                };
+
+                using var response = await _httpClient.SendAsync(request, HttpCompletionOption.ResponseHeadersRead, cancellationToken).ConfigureAwait(false);
+
+                if (response.IsSuccessStatusCode)
+                    return new CallStreamTestResult(true, "Connection succeeded.", (int)response.StatusCode);
+
+                if (response.StatusCode == System.Net.HttpStatusCode.Unauthorized || response.StatusCode == System.Net.HttpStatusCode.Forbidden)
+                    return new CallStreamTestResult(false, "Authentication failed. Check Basic Auth username and password.", (int)response.StatusCode);
+
+                var body = await SafeReadBodyAsync(response, cancellationToken).ConfigureAwait(false);
+                var message = $"Request failed with HTTP {(int)response.StatusCode}.";
+                if (!string.IsNullOrWhiteSpace(body))
+                    message += " " + body;
+
+                return new CallStreamTestResult(false, message, (int)response.StatusCode);
+            }
+            catch (OperationCanceledException)
+            {
+                return new CallStreamTestResult(false, "Connection test canceled.");
+            }
+            catch (Exception ex)
+            {
+                return new CallStreamTestResult(false, $"Connection test error: {ex.Message}");
+            }
+        }
+
+        private static async Task<string> SafeReadBodyAsync(HttpResponseMessage response, CancellationToken cancellationToken)
+        {
+            try
+            {
+                return await response.Content.ReadAsStringAsync(cancellationToken).ConfigureAwait(false);
+            }
+            catch
+            {
+                return string.Empty;
+            }
+        }
+
         public async IAsyncEnumerable<CallItem> GetCallStreamAsync([EnumeratorCancellation] CancellationToken cancellationToken)
         {
             while (!cancellationToken.IsCancellationRequested)
--- a/JoesScanner/ViewModels/SettingsViewModel.cs
+++ b/JoesScanner/ViewModels/SettingsViewModel.cs
@@ -1,6 +1,8 @@
 using System.Net.Http.Headers;
 using System.Text;
 using System.Text.Json;
+using Microsoft.Maui.ApplicationModel;
+using Microsoft.Maui.Graphics;
 
 namespace JoesScanner.ViewModels
 {
@@ -17,6 +19,7 @@ namespace JoesScanner.ViewModels
     public class SettingsViewModel : BaseViewModel
     {
         private readonly ISettingsService _settingsService;
+        private readonly ICallStreamService _callStreamService;
         private readonly MainViewModel _mainViewModel;
         private readonly HttpClient _httpClient;
 
@@ -151,7 +154,7 @@ namespace JoesScanner.ViewModels
 
         public ICommand ToggleBasicAuthPasswordVisibilityCommand { get; }
+        public ICommand TestBasicAuthCommand { get; }
 
         private bool _isBasicAuthPasswordHidden = true;
         public bool IsBasicAuthPasswordHidden
@@ -169,6 +172,103 @@ namespace JoesScanner.ViewModels
             }
         }
 
+        private bool _isTestingBasicAuth;
+        public bool IsTestingBasicAuth
+        {
+            get => _isTestingBasicAuth;
+            private set
+            {
+                if (_isTestingBasicAuth == value)
+                    return;
+
+                _isTestingBasicAuth = value;
+                OnPropertyChanged();
+
+                if (TestBasicAuthCommand is Command cmd)
+                    cmd.ChangeCanExecute();
+            }
+        }
+
+        private string _basicAuthTestStatusText = string.Empty;
+        public string BasicAuthTestStatusText
+        {
+            get => _basicAuthTestStatusText;
+            private set
+            {
+                if (_basicAuthTestStatusText == value)
+                    return;
+
+                _basicAuthTestStatusText = value;
+                OnPropertyChanged();
+                OnPropertyChanged(nameof(ShowBasicAuthTestStatus));
+            }
+        }
+
+        private Color _basicAuthTestStatusColor = Colors.Transparent;
+        public Color BasicAuthTestStatusColor
+        {
+            get => _basicAuthTestStatusColor;
+            private set
+            {
+                if (_basicAuthTestStatusColor == value)
+                    return;
+
+                _basicAuthTestStatusColor = value;
+                OnPropertyChanged();
+            }
+        }
+
+        public bool ShowBasicAuthTestStatus => !string.IsNullOrWhiteSpace(BasicAuthTestStatusText);
+
+        private void ClearBasicAuthTestStatus()
+        {
+            BasicAuthTestStatusText = string.Empty;
+            BasicAuthTestStatusColor = Colors.Transparent;
+        }
+
+        private async Task TestBasicAuthAsync()
+        {
+            if (IsTestingBasicAuth)
+                return;
+
+            ClearBasicAuthTestStatus();
+            IsTestingBasicAuth = true;
+            BasicAuthTestStatusText = "Testing connection";
+            BasicAuthTestStatusColor = Colors.Gray;
+
+            try
+            {
+                using var cts = new CancellationTokenSource(TimeSpan.FromSeconds(12));
+                var result = await _callStreamService.TestCallStreamAsync(cts.Token).ConfigureAwait(false);
+
+                MainThread.BeginInvokeOnMainThread(() =>
+                {
+                    BasicAuthTestStatusText = result.Message;
+                    BasicAuthTestStatusColor = result.Success ? Colors.Green : Colors.Red;
+                });
+            }
+            catch (Exception ex)
+            {
+                MainThread.BeginInvokeOnMainThread(() =>
+                {
+                    BasicAuthTestStatusText = $"Test error: {ex.Message}";
+                    BasicAuthTestStatusColor = Colors.Red;
+                });
+            }
+            finally
+            {
+                MainThread.BeginInvokeOnMainThread(() => IsTestingBasicAuth = false);
+            }
+        }
+
         public string BasicAuthUsername
         {
             get => _settingsService.BasicAuthUsername;
@@ -181,6 +281,8 @@ namespace JoesScanner.ViewModels
                 _settingsService.BasicAuthUsername = value;
                 OnPropertyChanged();
+
+                ClearBasicAuthTestStatus();
             }
         }
 
@@ -196,6 +298,8 @@ namespace JoesScanner.ViewModels
                 _settingsService.BasicAuthPassword = value;
                 OnPropertyChanged();
+
+                ClearBasicAuthTestStatus();
             }
         }
 
@@ -225,7 +329,7 @@ namespace JoesScanner.ViewModels
         private void ToggleBasicAuthPasswordVisibility()
         {
             IsBasicAuthPasswordHidden = !IsBasicAuthPasswordHidden;
         }
 
-        public SettingsViewModel(ISettingsService settingsService, MainViewModel mainViewModel)
+        public SettingsViewModel(ISettingsService settingsService, ICallStreamService callStreamService, MainViewModel mainViewModel)
         {
             _settingsService = settingsService;
+            _callStreamService = callStreamService;
             _mainViewModel = mainViewModel;
             _httpClient = new HttpClient();
 
@@ -252,6 +356,7 @@ namespace JoesScanner.ViewModels
 
             ToggleBasicAuthPasswordVisibilityCommand = new Command(ToggleBasicAuthPasswordVisibility);
+            TestBasicAuthCommand = new Command(async () => await TestBasicAuthAsync(), () => !IsTestingBasicAuth);
         }
     }
 }
--- a/JoesScanner/Views/SettingsPage.xaml
+++ b/JoesScanner/Views/SettingsPage.xaml
@@ -421,6 +421,22 @@
                                 Command="{Binding ToggleBasicAuthPasswordVisibilityCommand}"
                                 VerticalOptions="Center" />
                         </Grid>
+
+                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" Margin="0,12,0,0">
+                            <Button Text="Test"
+                                    Command="{Binding TestBasicAuthCommand}"
+                                    IsEnabled="{Binding IsTestingBasicAuth, Converter={StaticResource InverseBooleanConverter}}" />
+
+                            <Grid Grid.Column="1" ColumnDefinitions="Auto,*" ColumnSpacing="8" VerticalOptions="Center">
+                                <ActivityIndicator IsRunning="{Binding IsTestingBasicAuth}"
+                                                   IsVisible="{Binding IsTestingBasicAuth}"
+                                                   VerticalOptions="Center" />
+                                <Label Grid.Column="1"
+                                       Text="{Binding BasicAuthTestStatusText}"
+                                       TextColor="{Binding BasicAuthTestStatusColor}"
+                                       LineBreakMode="WordWrap"
+                                       IsVisible="{Binding ShowBasicAuthTestStatus}" />
+                            </Grid>
+                        </Grid>
                     </VerticalStackLayout>
                 </Frame>
