<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="JoesScanner.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:JoesScanner.ViewModels"
    xmlns:controls="clr-namespace:JoesScanner.Views.Controls"
    xmlns:models="clr-namespace:JoesScanner.Models"
    x:Name="Page"
    x:DataType="vm:HistoryViewModel"
    Title="History"
    BackgroundColor="{StaticResource TabStripNavy}"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False">

    <Grid Padding="5,0,5,5" RowDefinitions="0,*" RowSpacing="0">

        <Border
            Grid.Row="1"
            StrokeThickness="0"
            BackgroundColor="{AppThemeBinding Light=White, Dark=#020617}"
            VerticalOptions="FillAndExpand"
            HorizontalOptions="FillAndExpand">

            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>

            <Grid Padding="2" RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="8">

                <!-- Filters -->
                <Border
                    Grid.Row="0"
                    StrokeThickness="0"
                    BackgroundColor="#0D0D28"
                    Padding="2,0"
                    HorizontalOptions="Fill">

                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10" />
                    </Border.StrokeShape>

	                    <Grid Padding="8,6" RowDefinitions="Auto,Auto" RowSpacing="8">


<Grid Grid.Row="0" ColumnDefinitions="*,*,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto" ColumnSpacing="8">
	                            <VerticalStackLayout Spacing="2">
	                                <Label Text="Receiver" FontSize="12" TextColor="White" HorizontalTextAlignment="Center" HorizontalOptions="Fill" />
	                                <Border
	                                    BackgroundColor="White"
	                                    Stroke="#1f2937"
	                                    StrokeThickness="1"
	                                    Padding="8,6"
	                                    HeightRequest="34"
	                                    HorizontalOptions="Fill">
	                                    <Border.StrokeShape>
	                                        <RoundRectangle CornerRadius="12" />
	                                    </Border.StrokeShape>
	                                    <Border.GestureRecognizers>
	                                        <TapGestureRecognizer Tapped="OnReceiverTapped" />
	                                    </Border.GestureRecognizers>
	                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6" VerticalOptions="Center">
	                                        <Label
	                                            Text="{Binding SelectedReceiverDisplay}"
	                                            FontSize="13"
	                                            TextColor="Black"
	                                            LineBreakMode="TailTruncation"
	                                            VerticalOptions="Center" />
	                                    </Grid>
	                                </Border>
	                            </VerticalStackLayout>

	                            <VerticalStackLayout Grid.Column="1" Spacing="2">
	                                <Label Text="Site" FontSize="12" TextColor="White" HorizontalTextAlignment="Center" HorizontalOptions="Fill" />
	                                <Border
	                                    BackgroundColor="White"
	                                    Stroke="#1f2937"
	                                    StrokeThickness="1"
	                                    Padding="8,6"
	                                    HeightRequest="34"
	                                    HorizontalOptions="Fill">
	                                    <Border.StrokeShape>
	                                        <RoundRectangle CornerRadius="12" />
	                                    </Border.StrokeShape>
	                                    <Border.GestureRecognizers>
	                                        <TapGestureRecognizer Tapped="OnSiteTapped" />
	                                    </Border.GestureRecognizers>
	                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6" VerticalOptions="Center">
	                                        <Label
	                                            Text="{Binding SelectedSiteDisplay}"
	                                            FontSize="13"
	                                            TextColor="Black"
	                                            LineBreakMode="TailTruncation"
	                                            VerticalOptions="Center" />
	                                    </Grid>
	                                </Border>
	                            </VerticalStackLayout>

	                            <VerticalStackLayout Grid.Column="2" Spacing="2">
	                                <Label Text="Talkgroup" FontSize="12" TextColor="White" HorizontalTextAlignment="Center" HorizontalOptions="Fill" />
	                                <Border
	                                    BackgroundColor="White"
	                                    Stroke="#1f2937"
	                                    StrokeThickness="1"
	                                    Padding="8,6"
	                                    HeightRequest="34"
	                                    HorizontalOptions="Fill">
	                                    <Border.StrokeShape>
	                                        <RoundRectangle CornerRadius="12" />
	                                    </Border.StrokeShape>
	                                    <Border.GestureRecognizers>
	                                        <TapGestureRecognizer Tapped="OnTalkgroupTapped" />
	                                    </Border.GestureRecognizers>
	                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6" VerticalOptions="Center">
	                                        <Label
	                                            Text="{Binding SelectedTalkgroupDisplay}"
	                                            FontSize="13"
	                                            TextColor="Black"
	                                            LineBreakMode="TailTruncation"
	                                            VerticalOptions="Center" />
	                                    </Grid>
	                                </Border>
	                            </VerticalStackLayout>
	                        </Grid>

                        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="8" RowSpacing="6" VerticalOptions="Center">

                            <HorizontalStackLayout Grid.Row="0" Grid.Column="0" Spacing="4" VerticalOptions="Center" HorizontalOptions="Center">
                                <Label
                                    Text="Start time:"
                                    FontSize="12"
                                    TextColor="White"
                                    VerticalTextAlignment="Center"
                                    VerticalOptions="Center" />

                                <Border
                                    BackgroundColor="White"
                                    Stroke="#1f2937"
                                    StrokeThickness="1"
                                    Padding="0"
                                    HeightRequest="32"
                                    WidthRequest="100"
                                    HorizontalOptions="Start">

                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="12" />
                                    </Border.StrokeShape>

                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnTimeBorderTapped" />
                                    </Border.GestureRecognizers>

                                    <Grid>
                                        <Label
                                            Text="{Binding SelectedTimeDisplay}"
                                            FontSize="13"
                                            TextColor="Black"
                                            HorizontalTextAlignment="Start"
                                            VerticalTextAlignment="Center"
                                            Margin="6,0,0,0" />

                                        <!-- Keep the native picker for interaction, but hide its stretched visuals. -->
                                        <TimePicker
                                            x:Name="HistoryTimePicker"
                                            Time="{Binding SelectedTime, Mode=TwoWay}"
                                            Format="HH:mm"
                                            InputTransparent="False"
                                            Opacity="0.01"
                                            BackgroundColor="Transparent"
                                            TextColor="Transparent"
                                            HorizontalOptions="Fill"
                                            VerticalOptions="Fill" />
                                    </Grid>
                                </Border>
                            </HorizontalStackLayout>

                            <HorizontalStackLayout
                                Grid.Row="1"
                                Grid.Column="0"
                                Spacing="6"
                                VerticalOptions="Center"
                                HorizontalOptions="Center"
                                IsVisible="{Binding CanPickDateRange}">

                                <Label
                                    Text="Date:"
                                    FontSize="12"
                                    TextColor="White"
                                    VerticalTextAlignment="Center"
                                    VerticalOptions="Center" />

                                <Border
                                    BackgroundColor="White"
                                    Stroke="#1f2937"
                                    StrokeThickness="1"
                                    Padding="0"
                                    HeightRequest="32"
                                    WidthRequest="100"
                                    HorizontalOptions="Start">

                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="12" />
                                    </Border.StrokeShape>

                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnDateFromBorderTapped" />
                                    </Border.GestureRecognizers>

                                    <Grid>
                                        <Label
                                            Text="{Binding SelectedDateFromDisplay}"
                                            FontSize="13"
                                            TextColor="Black"
                                            HorizontalTextAlignment="Start"
                                            VerticalTextAlignment="Center"
                                            Margin="6,0,0,0" />

                                        <DatePicker
                                            x:Name="HistoryDateFromPicker"
                                            Date="{Binding SelectedDateFrom, Mode=TwoWay}"
                                            Format="M/d/yyyy"
                                            InputTransparent="False"
                                            Opacity="0.01"
                                            BackgroundColor="Transparent"
                                            TextColor="Transparent"
                                            HorizontalOptions="Fill"
                                            VerticalOptions="Fill" />
                                    </Grid>
                                </Border>

                                <Label
                                    Text="to"
                                    FontSize="12"
                                    TextColor="White"
                                    VerticalTextAlignment="Center"
                                    VerticalOptions="Center" />

                                <Border
                                    BackgroundColor="White"
                                    Stroke="#1f2937"
                                    StrokeThickness="1"
                                    Padding="0"
                                    HeightRequest="32"
                                    WidthRequest="100"
                                    HorizontalOptions="Start">

                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="12" />
                                    </Border.StrokeShape>

                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnDateToBorderTapped" />
                                    </Border.GestureRecognizers>

                                    <Grid>
                                        <Label
                                            Text="{Binding SelectedDateToDisplay}"
                                            FontSize="13"
                                            TextColor="Black"
                                            HorizontalTextAlignment="Start"
                                            VerticalTextAlignment="Center"
                                            Margin="6,0,0,0" />

                                        <DatePicker
                                            x:Name="HistoryDateToPicker"
                                            Date="{Binding SelectedDateTo, Mode=TwoWay}"
                                            Format="M/d/yyyy"
                                            InputTransparent="False"
                                            Opacity="0.01"
                                            BackgroundColor="Transparent"
                                            TextColor="Transparent"
                                            HorizontalOptions="Fill"
                                            VerticalOptions="Fill" />
                                    </Grid>
                                </Border>
                            </HorizontalStackLayout>

                            <Border
                                Grid.Row="0"
                                Grid.Column="1"
                                Grid.RowSpan="2"
                                BackgroundColor="Transparent"
                                Stroke="White"
                                StrokeThickness="1"
                                Padding="12,6"
                                HeightRequest="32"
                                WidthRequest="86"
                                HorizontalOptions="End"
                                VerticalOptions="Center">

                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>

                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SearchCommand}" />
                                </Border.GestureRecognizers>

                                <Label
                                    Text="Search"
                                    FontSize="12"
                                    TextColor="White"
                                    HorizontalTextAlignment="Center"
                                    VerticalTextAlignment="Center" />
                            </Border>
                        </Grid>

                    </Grid>
                </Border>

                <!-- Media controls -->
                <Border
                    Grid.Row="1"
                    StrokeThickness="1"
                    Stroke="#1f2937"
                    BackgroundColor="{StaticResource TabStripNavy}"
                    Padding="6"
                    HorizontalOptions="Fill">

                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16" />
                    </Border.StrokeShape>

                    <HorizontalStackLayout Spacing="10" HorizontalOptions="Center" VerticalOptions="Center">

                        <ImageButton
                            Source="mc_previous.png"
                            Command="{Binding PreviousCommand}"
                            IsEnabled="{Binding IsHistoryMediaButtonsEnabled}"
                            BackgroundColor="Transparent"
                            HeightRequest="38"
                            WidthRequest="38"
                            Padding="0"
                            ToolTipProperties.Text="Previous" />

                        <ImageButton
                            Source="mc_play.png"
                            IsVisible="{Binding ShowHistoryPlayButton}"
                            Command="{Binding PlayCommand}"
                            IsEnabled="{Binding IsPlayEnabled}"
                            BackgroundColor="Transparent"
                            HeightRequest="38"
                            WidthRequest="38"
                            Padding="0"
                            ToolTipProperties.Text="Play" />

                        <ImageButton
                            Source="mc_stop.png"
                            IsVisible="{Binding ShowHistoryStopButton}"
                            Command="{Binding StopCommand}"
                            IsEnabled="{Binding IsStopEnabled}"
                            BackgroundColor="Transparent"
                            HeightRequest="38"
                            WidthRequest="38"
                            Padding="0"
                            ToolTipProperties.Text="Stop" />

                        <ImageButton
                            Source="mc_next.png"
                            Command="{Binding NextCommand}"
                            IsEnabled="{Binding IsHistoryMediaButtonsEnabled}"
                            BackgroundColor="Transparent"
                            HeightRequest="38"
                            WidthRequest="38"
                            Padding="0"
                            ToolTipProperties.Text="Next" />

                        <ImageButton
                            Source="mc_speeddown.png"
                            Command="{Binding PlaybackSpeedDownCommand}"
                            IsEnabled="{Binding IsHistoryMediaButtonsEnabled}"
                            BackgroundColor="Transparent"
                            HeightRequest="38"
                            WidthRequest="38"
                            Padding="0"
                            ToolTipProperties.Text="Speed down" />

                        <ImageButton
                            Source="mc_speedup.png"
                            Command="{Binding PlaybackSpeedUpCommand}"
                            IsEnabled="{Binding IsHistoryMediaButtonsEnabled}"
                            BackgroundColor="Transparent"
                            HeightRequest="38"
                            WidthRequest="38"
                            Padding="0"
                            ToolTipProperties.Text="Speed up" />

                        <Label
                            Text="{Binding HistoryPlaybackSpeedLabel}"
                            FontSize="12"
                            TextColor="#e5e7eb"
                            VerticalOptions="Center"
                            Margin="6,0,0,0" />

                    </HorizontalStackLayout>
                </Border>

                <Label
                    Grid.Row="2"
                    Text="{Binding StatusText}"
                    FontSize="12"
                    TextColor="{AppThemeBinding Light=#374151, Dark=#e5e7eb}"
                    LineBreakMode="WordWrap" /><!-- Calls -->
                <CollectionView
                    x:Name="CallsList"
                    Grid.Row="4"
                    ItemsSource="{Binding Calls}"
                    SelectionMode="None"
                    Scrolled="OnCallsScrolled"
                    VerticalScrollBarVisibility="Never">

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:CallItem">
                            <Border
                                StrokeThickness="1"
                                Stroke="#1f2937"
                                BackgroundColor="{AppThemeBinding Light=White, Dark=#0b1220}"
                                Padding="8"
                                Margin="0,0,0,6">

                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="14" />
                                </Border.StrokeShape>

                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding IsToneHot}" Value="True">
                                        <Setter Property="StrokeThickness" Value="2" />
                                        <Setter Property="Stroke" Value="#ef4444" />
                                    </DataTrigger>

                                    <DataTrigger TargetType="Border" Binding="{Binding IsPlaying}" Value="True">
                                        <Setter Property="StrokeThickness" Value="2" />
                                        <Setter Property="Stroke" Value="#22c55e" />
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#ecfdf5, Dark=#022c22}" />
                                    </DataTrigger>
                                </Border.Triggers>

                                                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">

                                    <VerticalStackLayout Grid.Column="0" Spacing="2">

                                        <Label FontSize="14">
                                            <Label.GestureRecognizers>
                                                <TapGestureRecognizer Tapped="OnCallPrimaryTapped" CommandParameter="{Binding .}" />
                                            </Label.GestureRecognizers>
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="{Binding TimeOnlyDisplay}" TextColor="{AppThemeBinding Light=#111827, Dark=#e5e7eb}" />
                                                    <Span Text=" > " TextColor="{AppThemeBinding Light=#4b5563, Dark=#9ca3af}" />
                                                    <Span Text="{Binding ReceiverName}" TextColor="{AppThemeBinding Light=#111827, Dark=#e5e7eb}" />
                                                    <Span Text=" > " TextColor="{AppThemeBinding Light=#4b5563, Dark=#9ca3af}" />
                                                    <Span Text="{Binding Site}" TextColor="{AppThemeBinding Light=#111827, Dark=#e5e7eb}" />
                                                    <Span Text=" > " TextColor="{AppThemeBinding Light=#4b5563, Dark=#9ca3af}" />
                                                    <Span Text="{Binding Talkgroup}" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#111827, Dark=#e5e7eb}" />
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>

                                        <Label
                                            FontSize="12"
                                            TextColor="{AppThemeBinding Light=#374151, Dark=#cbd5e1}"
                                            LineBreakMode="WordWrap"
                                            Text="{Binding Transcription}">
                                            <Label.GestureRecognizers>
                                                <TapGestureRecognizer Tapped="OnCallPrimaryTapped" CommandParameter="{Binding .}" />
                                            </Label.GestureRecognizers>
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding Transcription}" Value="">
                                                    <Setter Property="IsVisible" Value="False" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>

                                        <Grid
                                            Margin="0,2,0,0"
                                            ColumnDefinitions="*,Auto"
                                            RowDefinitions="Auto"
                                            IsVisible="{Binding HasDetectedAddress}">

                                            <Label
                                                Grid.Column="0"
                                                FontSize="12"
                                                TextColor="{AppThemeBinding Light=#1d4ed8, Dark=#60a5fa}"
                                                TextDecorations="Underline"
                                                LineBreakMode="WordWrap"
                                                Text="{Binding DetectedAddress}">
                                                <Label.GestureRecognizers>
                                                    <TapGestureRecognizer
                                                        Tapped="OnAddressLinkTapped"
                                                        CommandParameter="{Binding .}" />
                                                </Label.GestureRecognizers>
                                                <Label.Triggers>
                                                    <DataTrigger TargetType="Label" Binding="{Binding DetectedAddress}" Value="">
                                                        <Setter Property="IsVisible" Value="False" />
                                                    </DataTrigger>
                                                </Label.Triggers>
                                            </Label>

                                            <Label
                                                Grid.Column="1"
                                                Margin="8,0,0,0"
                                                VerticalTextAlignment="Center"
                                                FontSize="{OnPlatform WinUI=10, MacCatalyst=10, Android=9, iOS=9}"
                                                FontAutoScalingEnabled="False"
                                                TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                                                LineBreakMode="NoWrap"
                                                MaxLines="1"
                                                Text="{Binding DetectedAddressConfidencePercent, StringFormat='{0}%'}" />
                                        </Grid>

                                        <VerticalStackLayout Spacing="1">
                                            <VerticalStackLayout.Triggers>
                                                <DataTrigger TargetType="VerticalStackLayout" Binding="{Binding What3WordsAddress}" Value="">
                                                    <Setter Property="IsVisible" Value="False" />
                                                </DataTrigger>
                                                <DataTrigger TargetType="VerticalStackLayout" Binding="{Binding Source={x:Reference Page}, Path=BindingContext.What3WordsLinksEnabled}" Value="False">
                                                    <Setter Property="IsVisible" Value="False" />
                                                </DataTrigger>
                                            </VerticalStackLayout.Triggers>

                                            <Label
                                                FontSize="{OnPlatform WinUI=10, MacCatalyst=10, Android=9, iOS=9}"
                                                FontAutoScalingEnabled="False"
                                                TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                                                LineBreakMode="NoWrap"
                                                MaxLines="1"
                                                Text="what3words" />

                                            <HorizontalStackLayout Spacing="10">
                                                <Label
                                                    FontSize="12"
                                                    TextColor="{AppThemeBinding Light=#2563eb, Dark=#60a5fa}"
                                                    TextDecorations="Underline"
                                                    LineBreakMode="WordWrap"
                                                    Text="{Binding What3WordsAddress}">
                                                    <Label.GestureRecognizers>
                                                        <TapGestureRecognizer
                                                            Tapped="OnWhat3WordsLinkTapped"
                                                            CommandParameter="{Binding .}" />
                                                    </Label.GestureRecognizers>
                                                </Label>

                                                <Label
                                                    FontSize="{OnPlatform WinUI=10, MacCatalyst=10, Android=9, iOS=9}"
                                                    FontAutoScalingEnabled="False"
                                                    TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                                                    TextDecorations="Underline"
                                                    VerticalOptions="Center"
                                                    Text="Copy">
                                                    <Label.GestureRecognizers>
                                                        <TapGestureRecognizer
                                                            Tapped="OnWhat3WordsCopyTapped"
                                                            CommandParameter="{Binding .}" />
                                                    </Label.GestureRecognizers>
                                                </Label>
                                            </HorizontalStackLayout>

                                            <HorizontalStackLayout
                                                IsVisible="{Binding HasWhat3WordsCoordinates}"
                                                Spacing="10">

                                                <Label
                                                    FontSize="{OnPlatform WinUI=10, MacCatalyst=10, Android=9, iOS=9}"
                                                    FontAutoScalingEnabled="False"
                                                    TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                                                    LineBreakMode="NoWrap"
                                                    MaxLines="1"
                                                    Text="{Binding What3WordsCoordinatesText}" />

                                                <Label
                                                    FontSize="{OnPlatform WinUI=10, MacCatalyst=10, Android=9, iOS=9}"
                                                    FontAutoScalingEnabled="False"
                                                    TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                                                    TextDecorations="Underline"
                                                    VerticalOptions="Center"
                                                    Text="Copy coords">
                                                    <Label.GestureRecognizers>
                                                        <TapGestureRecognizer
                                                            Tapped="OnWhat3WordsCoordsCopyTapped"
                                                            CommandParameter="{Binding .}" />
                                                    </Label.GestureRecognizers>
                                                </Label>

                                            </HorizontalStackLayout>

                                        </VerticalStackLayout>

                                    </VerticalStackLayout>

                                    <VerticalStackLayout Grid.Column="1" Spacing="2" HorizontalOptions="End" VerticalOptions="Start">

                                        <Label
                                            FontSize="12"
                                            TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                                            Text="{Binding DateDisplay}"
                                            HorizontalTextAlignment="End" />

                                        <Label
                                            FontSize="12"
                                            TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                                            Text="{Binding DurationMmSsDisplay}"
                                            HorizontalTextAlignment="End" />

                                        <Button
                                            Text="Grab"
                                            Style="{StaticResource PillButtonStyle}"
                                            BackgroundColor="{StaticResource TabStripNavy}"
                                            BorderColor="{StaticResource TabStripNavy}"
                                            TextColor="White"
                                            CommandParameter="{Binding .}"
                                            Clicked="OnCallDownloadClicked"
                                            HorizontalOptions="End" />

                                    </VerticalStackLayout>

                                </Grid>

                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>


            </Grid>
        </Border>
    
<!-- Address alert stack (max 3). Tap to jump to call, swipe or X to dismiss. -->
<VerticalStackLayout
    Grid.Row="1"
    Margin="8"
    VerticalOptions="End"
    HorizontalOptions="Fill"
    Spacing="6"
    ZIndex="99"
    IsVisible="{Binding HasAddressAlerts}">

        <VerticalStackLayout
        BindableLayout.ItemsSource="{Binding AddressAlerts}"
        Spacing="6">

        <BindableLayout.ItemTemplate>
            <DataTemplate x:DataType="models:CallItem">
                <SwipeView>
                    <SwipeView.RightItems>
                        <SwipeItems Mode="Execute">
                            <SwipeItem
                                Text="Dismiss"
                                BackgroundColor="{AppThemeBinding Light=#ef4444, Dark=#b91c1c}"
                                Invoked="OnAlertDismissInvoked"
                                CommandParameter="{Binding .}" />
                        </SwipeItems>
                    </SwipeView.RightItems>

                    <Border
                        Padding="10"
                        BackgroundColor="{AppThemeBinding Light=#f8fafc, Dark=#0b1220}"
                        Stroke="{AppThemeBinding Light=#cbd5e1, Dark=#334155}"
                        StrokeThickness="1">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10" />
                        </Border.StrokeShape>

                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnAlertTapped" CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>

                        <Grid RowDefinitions="Auto,Auto">
                            <Label
                                Grid.Row="0"
                                FontSize="{OnPlatform WinUI=12, MacCatalyst=12, Android=11, iOS=11}"
                                FontAutoScalingEnabled="False"
                                LineBreakMode="TailTruncation"
                                MaxLines="2"
                                TextColor="{AppThemeBinding Light=#0f172a, Dark=#e5e7eb}"
                                Margin="0,0,22,0"
                                Text="{Binding DetectedAddress}" />

                            <Label
                                Grid.Row="1"
                                FontSize="{OnPlatform WinUI=10, MacCatalyst=10, Android=9, iOS=9}"
                                FontAutoScalingEnabled="False"
                                LineBreakMode="TailTruncation"
                                MaxLines="1"
                                TextColor="{AppThemeBinding Light=#475569, Dark=#9ca3af}"
                                Margin="0,0,22,0"
                                Text="{Binding Talkgroup}" />

                            <Label
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                FontSize="16"
                                FontAutoScalingEnabled="False"
                                TextColor="{AppThemeBinding Light=#64748b, Dark=#9ca3af}"
                                HorizontalTextAlignment="Center"
                                VerticalTextAlignment="Center"
                                HorizontalOptions="End"
                                VerticalOptions="Start"
                                Margin="0,-2,-2,0"
                                Text="✕">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnAlertDismissTapped" CommandParameter="{Binding .}" />
                                </Label.GestureRecognizers>
                            </Label>
                        </Grid>
                    </Border>
                </SwipeView>
            </DataTemplate>
        </BindableLayout.ItemTemplate>
    </VerticalStackLayout>
</VerticalStackLayout>

</Grid>
</ContentPage>