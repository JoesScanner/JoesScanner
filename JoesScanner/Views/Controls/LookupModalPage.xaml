<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="JoesScanner.Views.Controls.LookupModalPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.NavBarIsVisible="False"
    BackgroundColor="#0D0D28">

    <Grid RowDefinitions="Auto,*" Padding="12" RowSpacing="10">

        <Grid RowDefinitions="Auto" ColumnDefinitions="*,Auto" ColumnSpacing="10">
            <Label
                x:Name="TitleLabel"
                Text="Select"
                FontSize="18"
                FontAttributes="Bold"
                TextColor="White"
                VerticalTextAlignment="Center" />

            <Border
                Grid.Column="1"
                BackgroundColor="Transparent"
                Stroke="White"
                StrokeThickness="1"
                Padding="12,6"
                HeightRequest="34"
                MinimumWidthRequest="90"
                HorizontalOptions="End">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="12" />
                </Border.StrokeShape>

                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnCancelTapped" />
                </Border.GestureRecognizers>

                <Label
                    x:Name="CancelLabel"
                    Text="Cancel"
                    FontSize="12"
                    TextColor="White"
                    HorizontalTextAlignment="Center"
                    VerticalTextAlignment="Center" />
            </Border>
        </Grid>

        <Border Grid.Row="1" BackgroundColor="White" Stroke="#1f2937" StrokeThickness="1" Padding="8">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="14" />
            </Border.StrokeShape>

            <Grid RowDefinitions="Auto,*" RowSpacing="8">

                <SearchBar
                    x:Name="Search"
                    Placeholder="Search"
                    TextChanged="OnSearchTextChanged"
                    BackgroundColor="White"
                    TextColor="Black" />

                <CollectionView
                    Grid.Row="1"
                    x:Name="List"
                    SelectionMode="Single"
                    SelectionChanged="OnSelectionChanged">

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="10,8">
                                <Label
                                    Text="{Binding .}"
                                    FontSize="14"
                                    TextColor="Black"
                                    LineBreakMode="TailTruncation" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>
        </Border>

    </Grid>

</ContentPage>
