<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="JoesScanner.Views.StatsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:JoesScanner.Views.Controls"
    xmlns:models="clr-namespace:JoesScanner.Models"
    Title="Stats"
    BackgroundColor="{StaticResource TabStripNavy}"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False">

    <Grid Padding="5,0,5,5" RowDefinitions="0,*" RowSpacing="0">

        <Border
            Grid.Row="1"
            StrokeThickness="0"
            BackgroundColor="{AppThemeBinding Light=White, Dark=#020617}"
            VerticalOptions="FillAndExpand"
            HorizontalOptions="FillAndExpand"
            Padding="0">

            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>

            <Grid Padding="2" RowDefinitions="Auto,*" RowSpacing="8">

                <Border
                    Grid.Row="0"
                    BackgroundColor="#0D0D28"
                    Padding="2,0"
                    VerticalOptions="Center"
                    HorizontalOptions="Fill">

                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10" />
                    </Border.StrokeShape>

                    <Grid Padding="8,6" ColumnDefinitions="*">
                        <Label
                            Text="Stats"
                            FontSize="18"
                            FontAttributes="Bold"
                            TextColor="White"
                            HorizontalTextAlignment="Center"
                            HorizontalOptions="Fill"
                            VerticalOptions="Center" />
                    </Grid>
                </Border>

                <VerticalStackLayout Grid.Row="1" Spacing="10" VerticalOptions="Center" HorizontalOptions="Center">
                    <Label
                        Text="Coming soon."
                        FontSize="14"
                        TextColor="{AppThemeBinding Light=#6b7280, Dark=#9ca3af}"
                        HorizontalOptions="Center" />
                </VerticalStackLayout>

            </Grid>
        </Border>
    </Grid>
</ContentPage>
