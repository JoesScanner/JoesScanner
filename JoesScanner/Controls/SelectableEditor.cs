using Microsoft.Maui.Controls;

namespace JoesScanner.Controls;

// A read only Editor that still allows text selection and copy.
// Uses a custom handler per platform to ensure selection works consistently.
public class SelectableEditor : Editor
{
}
